# neuralnetworks
This project explores the basics of neural networks using Keras and TensorFlow.  A simple fully connected neural network is trained on the MNIST handwritten digits dataset to perform digit classification.
